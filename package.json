{
  "private": true,
  "scripts": {
    "dev": "nuxt dev --dotenv env/.env.dev",
    "dev-test": "nuxt dev --dotenv env/.env.test",
    "dev-prod": "nuxt dev --dotenv env/.env.prod",
    "build": "nuxt build --dotenv env/.env.prod",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.14"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.6",
    "animate.css": "^4.1.1",
    "crypto-js": "^4.1.1",
    "dotenv": "^16.0.3",
    "reset-css": "^5.0.1",
    "sass": "^1.43.4",
    "zrender": "^5.4.1"
  }
}
